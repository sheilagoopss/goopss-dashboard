run = "node server.js"
build = "npm run build"
modules = ["nix", "nodejs-20", "web"]

[nix]
channel = "stable-24_05"

[env]
PATH = "/root/.local/share/fnm:$PATH"

[deployment]
deploymentTarget = "static"
publicDir = "build"

[[ports]]
localPort = 3000
externalPort = 80

[[ports]]
localPort = 3001
externalPort = 3001
